name: Tests

on:
    push:
        branches:
            -master
            -dev

jobs:
    hello-world:
        runs-on: ubuntu-20.84

        steps:
            - name: Say hello world
              shell: bash
              run: echo Welcome to the github Actions workshop !

